# 02 · 변경이력

> "왜·어떻게 지금에 왔는지" 시간순. 작업할 때마다 맨 아래에 **한 줄** 추가.

- **2026-06-01** 프로젝트 셋업: Next.js 16 + TypeScript + Tailwind v4로 `cbmc-app` 생성. 회원목록 뼈대 화면.
- **(초기)** 디자인 1차: DESIGN.md 따라 남색+금색+명조(Gowun Batang/Gothic A1) 적용.
- **2026-06-02** 디자인 전환: 사용자 결정으로 **파랑(#0066cc)+Pretendard 단일 폰트**로 전면 교체(금색·명조·그라데이션 폐기, 본문 17px). DESIGN.md는 구버전이 됨.
- **2026-06-02** Supabase 연결: `.env.local`, `lib/supabase/{client,server,middleware}.ts`. 매직링크 로그인 — PKCE 실패 후 **token_hash 방식**(`/auth/confirm`)으로 전환, 이메일 템플릿 수정.
- **2026-06-02** 0001: `profiles`+역할(admin/member/guest)+`is_admin()`+가입트리거+RLS. onjulyeo@gmail.com=admin.
- **2026-06-02** 0002: `chapters`(새서울 1행 seed: 금/오전7시/충현교회/회비 60·65만)·`members`(3층 분류)·`officer_terms`.
- **2026-06-02** 로그인 end-to-end 검증 완료(역할 뱃지 표시). 3단계 종료.
- **2026-06-02** 0003: 명단 임포트 1차(Handover.xlsx, 63명). 회원목록 실데이터 연결, `/members/new` 추가.
- **2026-06-02** 0004~0005: 명단 정제(53명)·전화번호 010- 정규화·배우자 쌍 매칭.
- **2026-06-02** 0006~0007: **상태 enum 변경** `활동중/유보/등록전/OB` → **`활동/휴면/비활동/OB`** (회원구분=등급 / 상태=활동도 2축 분리). 53명 재임포트.
- **회원표 고도화:** `@tanstack/react-table` 도입(정렬·검색·회원구분/태그 필터·열 토글). 증경회장 등은 **이름 옆 배지(tags)**로, 직책별 색 구분(`badgeClass`).
- **0008:** `meetings`(회차·모드)+`attendance`(출석·식대). **0009** events, **0010** archive(+storage), **0011** QR 체크인 함수, **0012~0013** 포스터 배경(+storage).
- **0014:** `transactions`(회계, A 메인/B 식대 트랙) + 카뱅 임포트 자동분류(`classifyTxn`). **0015** 식대 설정(chapters에 meal_*). **0016** 체크아웃.
- **0017~0018:** `announcements`(+포스터 이미지). **0019** meetings.program. **0020** members.photo_url(+storage).
- **콘텐츠:** AI 포스터 생성(Cloudflare Flux/Recraft, `/api/poster-bg`)·경조사 안내장 AI 추출(Gemini, `/api/occasion-extract`).
- **모임온(MoimON):** 디자인 시스템 명칭으로 도입. 범용 판매 랜딩 `Landing.tsx` 제작 → `/preview-landing`에만 연결(실서비스 동선 밖).
- **2026-06-10** **정본 통합 생성**: 흩어진 사양서 4종·DESIGN.md·design-briefs와 코드 충돌을 진단하고, "현재 코드=진실" 기준으로 `docs/report/정본/` 4종 작성. 새서울(운영) vs 모임온(미착수) 분리 현황 명시.
- **2026-06-10** **정본 보강(진단 기준선 반영)**: 하드코딩 수치 정밀화(쿼리 23 + insert/함수 7 ≈ 30곳), A/B/C 분리 결론을 01 §7로 재구성, "멀티테넌트 미행사"·"기획 범위 초과 구현" 명문화. 03에 명단 수 불명확(사양서 56 vs 코드 63→53)·모임온 4대 분기점 보강.
- **2026-06-10** **불명확 해소(마이그레이션 정밀 분석)**: anon 키는 RLS로 members 직접조회 불가 확인 → 임포트 파일이 `delete→insert` 교체 방식임을 근거로 **최종 회원 53명 확정**(56=사양서 추정·63=0003 1차·53=정제). status 제약 `활동/휴면/비활동/OB` 확정. **신규 발견:** grade 제약(0002: 5종)에 `유보회원` 없으나 앱 UI는 제공 → 마이그레이션 드리프트로 03에 🔴 기록 + 라이브 대조 SQL 첨부. 01에 실제 데이터 분포(등급·상태) 추가.
- **2026-06-10** **라이브 DB 대조로 드리프트 종결**: 임원이 Supabase SQL Editor로 실조회. 회원 **53명** 확정(상태 활동51·비활동1·휴면1 / 등급 정31·부부9·명예8·준4·**유보1**). 유보회원 행 실재 = 라이브 grade 제약이 6종 허용 → 판정 **(a) 수동 ALTER 적용됨**. **`0021_grade_add_yubo.sql` 생성**해 파일=DB 일치(소급 기록). 03의 🔴 grade 항목 해소. 남은 확인필요 = Vercel 배포 여부뿐.
- **2026-06-07~11** **디자인 전 화면 개편(claude.ai/design 핸드오프 이식)**: claude.ai/design에서 화면을 디자인 → 핸드오프 번들(README+chat+소스)을 받아 코드로 이식. 셸·대시보드·회원목록+상세·연간일정·출석·체크인·통계·회계(가져오기·내역·보고서)·콘텐츠·경조사·공지·회원홈·로그인 = **13개+첫화면 전부** "모임온 디자인 시스템"으로 재이식. 원칙 = **"새 디자인 + 기존 실기능 100% 보존" 머지**(시안이 빼먹은 식대 납부체크·미납 SMS·포스터 AI/스톡/보관함·일정 자동생성/이벤트 등은 살림). 패턴 = 화면별 `.moim-*` 스코프 CSS + 서버 로더/표현 컴포넌트 분리(`DashboardView`·`StatsView`·`RolesBoard`…). **삽질:** 다운로드 HTML(24MB)은 컴파일물이라 못 읽음 → 진짜 소스는 핸드오프 번들 안에 있음을 발견. 긴 회사명 nowrap이 표를 넘침 → 회원목록 셀 말줄임.
- **2026-06-11** **첫화면 = 새서울 환영(Welcome)**: 익명 `/`를 모임온 판매 랜딩 → **새서울 CBMC 환영+매직링크 로그인**(`Welcome.tsx`)으로 교체. `/login`도 같은 화면으로 통일. 로고는 클로드디자인 업로드분 추출 `public/cbmc-symbol.webp`. 헤더 브랜드 "ON 모임온" → **"CBMC 새서울지회"**. `Landing.tsx`는 `/preview-landing`에 보존(미래 판매 사이트용).
- **2026-06-11** **신규 기능**: ① **회원 사진**(`0020_member_photos.sql` — `members.photo_url` + 공개 버킷 `member-photos`, 상세 드로어에서 업로드/삭제) ② **회원 태그 편집**(프리셋 칩 토글 + 직접추가, 저장 시 `tags[]`) ③ **미납자 SMS 버튼**(`sms:` 프리필, 자동발송 아님) ④ **역할관리 화면**(`/roles`·`RolesBoard` — 기존 profiles RLS 활용, 추가 SQL 없이 임원이 권한 지정).
- **2026-06-11** **Vercel 운영 배포 확정**: `git push origin main` + `npx vercel --prod`로 **`cbmc-app.vercel.app` 라이브**. (03의 "배포 여부 확인필요" 해소)
- **2026-06-11** **모임온 SaaS 전략 결정**: CBMC용·일반용 따로가 아니라 **하나의 설정가능 멀티테넌트 제품**으로, 새서울과 **완전 분리**(새 이름·새 repo). 새서울=고객1. **착수 전 PRD 먼저**(별도 챗/프로젝트에서). 회원 로그인은 기본 매직링크 rate limit 실증 → Gmail SMTP 또는 "회원 로그인 없이" 미결.
- **2026-06-11** **`/init` — CLAUDE.md 보강**: 정본 운영 룰은 보존하고, 코딩 에이전트용 **Commands**(build=검증 게이트·preview-shell 패턴)와 **Orientation**(RLS/role·chapter_id 멀티테넌트 씨앗·수동 마이그레이션·`.moim-*` 스코프 CSS·lib 도메인 로직·API 라우트) 섹션을 추가. 아키텍처 상세는 중복 없이 `01-개요.md`를 가리키게 함.
- **2026-06-11** **`/정본` 갱신**: 위 디자인 개편·Welcome·역할관리·사진/태그/SMS·배포·모임온 전략을 코드 기준으로 정본 4종에 반영. 한장본 표지 현재상태 재작성, 01 §3(회원 화면=`MembersList`, `MembersTable` 레거시 명시)·§6(이중 토큰)·§7("디자인은 모임온, 제품은 새서울" + 전략 결정) 갱신, 03의 배포·로고 해소·이메일 실증·모임온 향후방향 재정리.
- **2026-06-16** **메뉴 주제별 묶음(②)**: `SiteNav` 임원 메뉴를 운영(대시보드·공지·콘텐츠)/모임(연간일정·출석식대·출석통계)/관리(회원관리·회계·아카이브)/설정(역할관리) 순서로 재배치, 그룹 사이 옅은 구분선(`nav-div`). 설계 문서 `docs/superpowers/specs/2026-06-16-admin-roles-member-access-design.md`.
- **2026-06-16** **메인/서브 관리자(①)**: `0022_owner_admin.sql` — `profiles.is_owner` + `is_owner()` 함수 + 역할변경 RLS를 **메인 전용**(`owner_update_all`)으로 교체 + `protect_owner` 트리거(앱에서 메인 강등·is_owner 변경 차단, SQL Editor는 통과). `/roles` 페이지·메뉴는 메인(owner)에게만 노출, 서브 임원에겐 숨김. `RolesBoard`는 메인 행 잠금(🔒)·"메인" 배지. onjulyeo=owner.
- **2026-06-16** **회원 접근 확장(⑤)**: `0023_members_directory.sql` — 제한 뷰 `members_directory`(이름·연락처·회사, authenticated만). 회원용 `/directory`(`DirectoryView`, 검색+전화걸기, 관심·비로그인 차단). 회원 메뉴=공지·회원명단·아카이브, 관심(guest)=공지만. `SiteNav` 역할 배지도 회원/관심 구분.
- **2026-06-16** **특별행사 콘텐츠(④)**: `ContentHub`에 **특별행사** 탭(`SpecialEventTool`) 추가 — 한국대회·송년회·봄소풍·수련회·기타 종류별 카톡 공지글 생성+복사+공지 게시(포스터는 기존 편집기 재사용). `0024_announcement_event_category.sql`로 공지 카테고리에 `행사` 추가.
- **2026-06-16** **아카이브 분류 정리(③)**: 사용자가 거창한 연표/역대임원 기능 대신 "카테고리 + 앱 업로드"로 단순화 결정. `ArchiveBoard` 분류를 `연혁·역대임원·행사사진·영상·문서·기타`로 교체(기존 연혁·역대회장·사진·문서·기타). **영상** 분류는 중앙회 CBMC 소개영상 등 링크용 — 카드에서 "▶ 영상 보기"로 표시, 링크 입력칸 안내문 분기. 업로드 기능은 기존 그대로(사진+필드, 임원만). archive.category는 제약 없는 text라 **SQL 불필요**.
