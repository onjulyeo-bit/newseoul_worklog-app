# 02 — 변경이력

> "왜/어떻게 지금에 왔는지" 시간순 한 줄 기록. 작업할 때마다 맨 위에 한 줄 추가.

- **2026-07-30** 게스트 체크인(`0055`) — 한 번만 오는 방문자를 **이름 없이 인원수로만** 집계(운영자가 실명단 보유). 회원 QR과 **같은 회차 토큰 재사용**(별도 QR 불필요). `guest_checkins` 테이블 + 토큰검증 보안RPC 3종(`guest_check_in`/`guest_check_out`/`guest_count`, anon +1·취소만). 체크인 화면 명단 아래 '처음 오셨나요? → 게스트로 체크인' 버튼(식대 포함 안내 모달·방금취소·현재 게스트 수). 운영진 출석 화면: 통계에 '게스트 N명' + 운영진용 ±버튼(스캔 안 한 게스트 수동 보정), 미납 안내 요약에 게스트 식대(=인원×식대) 표기. 게스트는 **무조건 식대 포함**. ⚠️ `0055` 수동 실행 필요(Supabase SQL Editor).
- **2026-06-19** 음성 자기소개 정확도 개선(받아쓰기 프롬프트 강화·맞춤법교정·temp 0.2) + **강사·간사 정보링크 자동저장**(`0041` instructor_self_upsert 익명 RPC, /instructor-form 공개폼·초록 OG·SiteNav 숨김, 강사간사 화면 '정보요청 링크'). + `/my-info` 전용 OG(파란 '내 정보 입력' 카드)로 앱 링크와 구분.
- **2026-06-19** 체크리스트 배치 — ▸회원관리 드로어 **회원 삭제**(removeMember, 엑셀 재업로드 불필요) ▸체크인·식대 회차 **시간순+당회(오늘/직전) 가운데 선택**(전·후 노출) ▸'미납 독촉 문구'→'미납 안내 문구', 운영진 '역할변경 불가'→'권한설정 불가', 권한설정 카카오 안내문에 이메일 나중수집 설명 ▸**연간결산을 회계→통계로 이동**(/attendance/annual, StatsTabs 3탭, AnnualBoard tabs 주입, /finance/annual 리다이렉트) ▸**강사·간사 양식(엑셀) 다운로드 + 업로드**(parseInstructorsXlsx·importInstructors, 이름 기준 새 인원만) ▸**음성 자기소개**(/api/intro-voice: 녹음→gemini-2.5-flash 전사·한두문장 정리→자기소개 칸, 자기입력 폼 MediaRecorder). 이미 있던 것 확인: 읽기 운영진 지정·계좌 앱추가.
- **2026-06-18** 자기소개 + 명함 이미지(`0039`) — members.intro(한 줄 자기소개)·business_card_url. **member-cards 공개 버킷**(익명 insert 허용 → 비로그인 자기입력 폼에서도 명함 업로드, 수정·삭제는 운영진). 회원관리 드로어 '자기소개·명함' 섹션(CardUpload), 자기입력 폼(/my-info) textarea(예문 placeholder)+명함 업로드, 회원명단(directory)에 자기소개 표시·명함 보기 버튼. 자기입력 RPC·members_directory 뷰 갱신.
- **2026-06-18** 회계 연간결산 신설(`0038`) + 통계는 회원수만(돈 분리) — '돈=회계 / 사람=통계' 일원화로 중복·혼선 제거. ▸통계 '연도별 회원등록 현황': 수입·후원금 제거 → 정·준·신입 **회원 수만**(추이 그래프). ▸회계 **'연간결산' 탭**(annual_finance): 연도별 **수입·지출 입력 → 이월금 자동(=수입−지출)**, **2026~ 거래 자동집계 버튼**(회비통장 입금·출금+전년이월), 인포그래픽(수입·지출 막대·수입구성 도넛), 2021~. 과거(2021~2025)는 결산서 보고 합계+이월만 간단 입력(사용자 선택). 검증: 2024 이월금 ₩2,366,311 결산서 일치.
- **2026-06-18** 연도별 회비 납부내역 + 연도별 회원등록 현황(결산 기반, `0037`) — ▸회계 '연도별 회비'→**'연도별 회비 납부내역'**(제목): **구분 컬럼·정/준 집계 삭제**, 순수 기록(이름×연도×금액·납부인원·합계)만. ▸통계 '회원등록현황'→**'연도별 회원등록 현황'**: 자동분류 대신 **연말 결산 확정값을 연도별 수기 입력**(annual_summary: 정/준/신입/회비수입/후원금, 읽기 is_staff·쓰기 is_admin). 납부내역(회비)·가입일 기반 **추정값 보조 버튼**(입력 도움, 확정은 수기). SVG 추이 그래프(정·준/회비·후원금), **2021년부터**, viewer 읽기전용. 근거: 과거 통장·업로드 데이터 오류 多 → 사람이 결산에서 확정한 숫자를 단일 진실로. 부부정회원 20만·30만 단독납부=준회원·명예회원 입금=후원금 등 예외는 결산 입력에서 사람이 반영.
- **2026-06-18** 회원관리 통계카드 휴면→VIP, 회원구분에 부부정회원 추가, 상태→회원구분 순서. 회원안내(directory `0036`) 공개항목 확장(이메일·직위·업종). 생일 양·음력 + 주소 집/회사 구분(`0035`) — members에 birth_calendar('양력'|'음력')·address_type('집'|'회사') 추가. 생일='실제 생일'(자유형식), 주소는 1개만 받고 집/회사 라벨만 선택(둘 다 X). 회원관리 편집(양/음력·주소구분 드롭다운)·보기·저장·엑셀, 자기입력 폼(/my-info)에 토글. 자기입력 RPC 재정의(인자 추가). parseMembersXlsx 인식(주소/주소구분 정확 매칭). 다음 검토대기: 연도별 회비 납부내역 제목·구분삭제, 통계 '연도별 회원등록 현황' 결산기반·2021~ (사용자 결정 대기).
- **2026-06-18** 회원 항목 추가(`0034`) — members에 **생년월일(birth_date)·주소(address)·출석교회(home_church)** 컬럼. 회원관리 드로어 보기·편집('연락·신앙' 섹션)·저장·엑셀 내보내기, 자기입력 폼(/my-info) 입력, parseMembersXlsx 인식(라운드트립). 자기입력 RPC(member_self_lookup/update)를 새 항목 포함해 재정의(반환·인자 변경 → drop 후 재생성). 이메일·가입일은 기존 유지. 주민번호 등 민감정보는 계속 미수집(생년월일은 text 자유형식).
- **2026-06-18** 회원등록현황 개편(통계) — 기존 '등록 현황'(통장거래 매칭)을 **회원등록현황**으로 재작성. 기준을 **업로드 연도별회비(annual_dues, 구분)** 로 전환(통장 연회비는 '교차확인' 보조 유지). **연도 선택**(클라 즉시 전환)+**회원구분 탭**(전체/정회원[부부정회원 2명 포함]/준회원/신입회원). 신입회원=회원 가입일(joined_on) 기준 월별 집계. **SVG 인포그래픽**(새 라이브러리 없음): 연도별 정·준회원 수(그룹막대)·연도별 회비 수입(막대)·연도 회원구분(도넛)·월별 신입(막대). StatsTabs 라벨 '등록 현황'→'회원등록현황'. 회계 '연도별 회비' 탭은 데이터 입력용으로 유지(업로드·수정). 공지 '내리기'=공지 상세 우측 상단 삭제(기존 기능).
- **2026-06-18** 읽기 운영진(`0032` viewer) — `profiles.role`에 `viewer` 추가 + `is_staff()`(admin|viewer). RLS를 **읽기=is_staff / 쓰기=is_admin** 로 분리(members·meetings·attendance·transactions·annual_dues·accounts·instructors) → viewer는 UI 무관하게 DB에서 수정 차단(안전). 페이지 가드 isStaff로 완화 + `canEdit` 전달(회원관리·대시보드·통계·등록현황·강사간사·계좌·연도별회비·거래·가져오기·연간일정·체크인). 회원관리/거래/체크인/일정=편집 컨트롤 숨김·'읽기 전용' 표시. 클라이언트 화면용 `lib/useCanEdit.ts`. SiteNav 운영 메뉴 노출+'읽기 운영진' 배지. 권한설정에 지정 옵션. members RLS 변경은 `members_directory`(definer 뷰)에 영향 없음(회원 명단 회귀 없음).
- **2026-06-18** 회원 자기입력 폼(`0031`) — `/my-info`(로그인 불필요). 이름+전화 뒷4자리 본인확인 → 개인정보(연락처·이메일·회사·직위·업종·배우자·차종·차번호)만 직접 수정 → 자동 저장. `member_self_lookup`/`member_self_update` SECURITY DEFINER RPC(anon)만 사용(members RLS 그대로). 등급·상태·태그는 못 바꿈. 회원관리 헤더에 '정보요청 링크' 복사 버튼(카톡방 배포용). 모바일 우선 단독 화면(SiteNav 숨김).
- **2026-06-17** 회계 계좌관리 탭(`0029` accounts) — 송금 계좌(중앙회·남부연합회·강사·간사·지회 등) 마스킹 표시(끝4자리)+클립보드 복사+잠깐보기, 운영진 전용 RLS. ⚠️계좌번호 저장=원칙의 의도적 예외(운영진만·마스킹). FinanceTabs에 탭 추가(NO_AXIS).
- **2026-06-17** 강사·간사 메뉴(`0030` instructors) — 회원과 분리된 풀(강사/간사·내부/외부·소속·연락처·전문분야·강사비메모), 필터·추가/삭제, 운영진 전용. SiteNav 회원관리 다음에 메뉴. 향후 연간일정 강사·회계 강사비 연동 여지.
- **2026-06-17** 회원관리 — 회원구분·상태 필터를 칩 단일선택(정회원만/활동만 클릭 1번), 통계 5칸(전체·정회원·준회원·가족회원·휴면), 상태 OB 삭제(활동=카톡방 명단 기준). 회원 통합본(71명)·연도별 회비(annual_dues, 0028) 업로드 변환 지원.
- **2026-06-16** 아카이브 정보허브 — `/archive` 허브(6섹션 카드) + `/archive/[section]`(섹션별 화면·운영진 CRUD). 기존 `archive` 테이블·버킷 재활용(SQL 불필요). 섹션=연혁(타임라인)·역대지회장(인물카드, 간단입력)·주요행사(갤러리)·입회안내(글)·자료실(문서 PDF·HWP 업로드+다운로드)·CBMC소개(영상·링크). `sections.ts` 설정. 공개=로그인 누구나(현행). 옛 단일 `ArchiveBoard` 제거. 메뉴 순서 업무흐름순(공지=콘텐츠 다음)+권한설정 앞 구분선.
- **2026-06-16** 회계 2축(회비 관리/식대 관리) — `FinanceAxis`(URL `?axis=fee|meal`). 가져오기=통장별 트랙 고정(회비→A/식대→B 자동분류), 내역=축별 필터(트랙 Seg 제거), 제목·안내 축 반영. **보고서는 통합 유지**(연회비현황+식대정산 한 보고서, 사용자 요청). `FinanceTabs` axis 유지.
- **2026-06-16** 정회원 등록 현황 — 통계에 탭(`StatsTabs`: 체크인 통계 / 등록 현황). `/attendance/registration`: 올해 '연회비' 입금 거래를 회원 이름과 매칭해 납부/미납·금액·판정(memberJudge) 표시 + 명단 외 입금 목록. SQL 불필요. SiteNav '체크인 통계'→'통계'(active 매칭 보정).
- **2026-06-16** 운영진 예정(`0027`) — `members.intended_role`('admin'). 명단 드로어 '운영진 예정' 토글·배지·엑셀(운영진예정 칸). 연결(자동/수동) 시 intended_role='admin'이면 자동 운영진. `handle_new_user`가 자동연결+예정권한 적용.
- **2026-06-16** 회계 거래 내보내기 — 거래 내역 화면에 엑셀/CSV 내보내기(현재 필터 결과, `toTxnRows` 한글헤더). 권한 정책 보강: 관심(기본)도 아카이브 열람, 회원명단(개인정보)만 회원 권한 필요(카카오 로그인 회원 놓침 방지).
- **2026-06-16** 권한·로그인 개편 A안 시작(4-1/7/8) — `0026`: `profiles.member_id`(로그인 계정↔명단 회원 연결) + `handle_new_user` 보강(이메일이 명단과 같으면 로그인 시 자동 연결 + role=member). `/권한설정`(RolesBoard) 재작성: **미연결 로그인**(카카오 등) ↔ 명단 회원 연결(이메일 일치 시 자동 추천·원클릭) + **연결됨**(권한 변경·연결 해제, 메인 잠금). 회원 명단 드로어·엑셀·내보내기에 **이메일 칸** 추가(차차 기록 가능). 로그인 폼 이메일 자동저장(localStorage). 설계: `docs/superpowers/specs/2026-06-16-auth-link-design.md`. #8답=서브도 매직링크/카카오, #7=세션 수주 유지(매번 로그인 불필요). 메인 카테고리 순서(6)는 사용자가 직접 정리 예정.
- **2026-06-16** 체크인 회차 정렬 — 현재/최근 회차를 앞에(기본 선택), 미래(12월 등)는 뒤로(`attendance/page.tsx`).
- **2026-06-16** 회원 내보내기·업로드 보강 — 회원관리(`MembersList`)에 **엑셀/CSV 내보내기 복구**(MembersTable 레거시화로 빠졌던 기능, `toExportRows` 한글헤더, 현재 필터 결과 기준). 엑셀 업로드 기본 모드를 **append(새 회원만 추가)**로(첫 옵션·권장), 미리보기에 상태 컬럼. 파서(`parseMembersXlsx`)가 **상태(status)·회원등록일·기타까지 읽도록** 수정(이전엔 status를 '활동' 고정) — 다운로드→엑셀 편집→재업로드 왕복 가능. 부부회원→가족회원·옛 상태값 자동 변환.
- **2026-06-16** `0025` 적용 — 회원구분 부부→가족, 상태 전원 활동 초기화, **members grade/status CHECK 제약 삭제**(값은 앱 드롭다운·RLS로 관리, 해마다 변동 유연). ⚠️교훈: 옛 CHECK가 새 값(가족회원/VIP)을 막으므로 **제약 DROP을 UPDATE보다 먼저** 해야 함(순서 안 지키면 23514). 한글 정규화 문제로 오진했었음.
- **2026-06-16** 명칭·회원스키마 일괄(묶음 A·B) — 출석체크→**체크인**(메뉴·제목), 권한명 **임원→운영진**, 역할관리→**권한설정**. 회원관리: 차량번호 검색, **등급→회원구분**, **부부회원→가족회원**, **상태 비활동 제거(→휴면)**, **등록 삭제+VIP 추가**(`0025`). `MembersTable`/`[id]`는 레거시(미사용) 확인. 아카이브 허브 디자인 브리프 작성(`design-briefs.md` ⑭). 권한·로그인 개편(4·7·8)·아카이브 빌드는 별도 트랙(03).
- **2026-06-16** 메인/서브 관리자(①) — `0022`: `profiles.is_owner`+`is_owner()`+역할변경 RLS를 메인 전용(`owner_update_all`)+`protect_owner` 트리거. `/roles` 메인 전용, 메인 행 잠금(🔒). onjulyeo=owner.
- **2026-06-16** 메뉴 주제별 묶음(②) — `SiteNav` 임원 메뉴를 운영/모임/관리/설정 순서 재배치+그룹 구분선(`nav-div`).
- **2026-06-16** 회원 접근 확장(⑤) — `0023`: 제한 뷰 `members_directory`(이름·연락처·회사). 회원용 `/directory`(`DirectoryView`). 회원=공지·명단·아카이브 / 관심=공지만. 역할 배지 회원·관심 구분.
- **2026-06-16** 특별행사 콘텐츠(④) — `ContentHub`에 특별행사 탭(`SpecialEventTool`: 한국대회·송년회 등 카톡 공지글). `0024`: 공지 카테고리에 `행사` 추가.
- **2026-06-16** 아카이브 분류 정리(③) — `ArchiveBoard` 분류 `연혁·역대임원·행사사진·영상·문서·기타`(영상=중앙회 소개영상 링크, "▶ 영상 보기"). 업로드 기존 유지, SQL 불필요.
- **2026-06-14** 정본 최초 생성(통합) — 현재 코드 기준으로 한장본·개요·변경이력·할일 작성.
- **2026-06-14** 카카오 로그인 프로덕션 라이브 — KOE205(Supabase가 account_email 강제 요청 + 개인 카카오앱 이메일 권한없음) 해결: 카카오 앱을 개인 개발자 비즈앱 전환 후 이메일 동의항목 "선택 동의" 설정. 코드 변경 없음.
- **2026-06-14** Vercel 자동배포 중단 사고 해결 — 원인: Vercel↔GitHub 연동 끊김(Vercel GitHub App의 select-repos 목록에서 `newseoul_worklog-app` 누락). 조치: 저장소 재연결 + GitHub App을 All repositories로 변경.
- **2026-06-14** 카카오 로그인 버튼 추가(`app/Welcome.tsx`) — `signInWithOAuth({provider:'kakao'})` + "카카오로 로그인" 버튼/구분선/CSS. 콜백·DB 재사용.
- **2026-06-11** 카카오 로그인 사전 진단 — 인증 구조 파악, 방향 결정(임원=이메일 유지, 회원=카카오).
- **2026-06-07** 역할 관리 화면 추가(`/roles`) — 임원이 권한 지정.
- **2026-06-07** 회원 상세 태그 편집, 회원목록 말줄임 등 회원관리 개선.
- **2026-06-20** 연간일정 회차 자료(포스터·영상) — meetings에 poster_url·recording_url(0047). 포스터는 해당 주차 공지 이미지를 '자동 매칭'(모임 6일전~다음날, 수동 지정 시 우선). 영상은 유튜브 미등록 등 링크만 저장(용량 0). 회원 카드에 '포스터'·'영상 보기' 버튼, 운영진은 회차별 '자료' 모달(포스터 업로드·영상 링크).
- **2026-06-20** 연간일정 회원 공개 — meetings 읽기 RLS를 로그인 전체로(0046, 쓰기는 운영진 유지), 회원/관심 메뉴에 '연간일정' 추가. 비운영진은 월별 카드 읽기뷰(월 헤더·모임수·형식/모드 뱃지·강사/사회·휴회·★행사·지난회차 디밍), 운영진은 기존 편집표 유지.
- **2026-06-20** 아카이브 인물 — 순서 변경 기능(순서 변경 모드 ◀순번▶, sort_order 컬럼 0049, saveArchiveOrder) + 인물 편집에 '사진 삭제'(카드 유지, image_url만 제거). 인물 목록 정렬을 sort_order 우선으로.
- **2026-06-20** 인물 카드 개별 편집 — 카드에 ✎ 버튼(이름·재임기간 수정 + 사진 추가/변경, 배경통일·AI 옵션). updateArchive 액션. 사진 없는 카드는 칸 클릭으로도 편집 진입.
- **2026-06-20** 인물 사진 — 카드 클릭 시 확대 보기 모달. 'AI 고급 배경(나노바나나=gemini-2.5-flash-image)' 옵션 추가(/api/photo-bg, 엣지 깔끔하게 배경 재생성). ※ 이미지 모델은 Gemini 무료등급 불가 → 결제 활성화 시 사용. 미사용 시 기존 @imgly 배경제거 유지.
- **2026-06-20** 사진 일괄 등록 개선 — 기존 인물에 사진 '연결' 모드(드롭다운 선택, 새 카드 중복 생성 방지, setArchiveImage). 인물 카드 아바타를 원형→포트레이트 둥근사각(얼굴 가독성). 배경 사진관 그라데이션 + 자동 보정.
- **2026-06-20** 사진 일괄 등록(역대지회장) — 아카이브 인물 섹션에 '사진 일괄 등록' 추가. 사람별 사진 여러 장 선택→파일명=이름 자동→브라우저 내 배경 제거·단색 통일(@imgly, 무료·로컬)→정사각 크롭→archive 일괄 등록. lib/photoProcess.ts(재사용).
- **2026-06-20** 운영 매뉴얼(/manual) 추가 — 운영진 전용. 실제 운영자료(예배순서·간사업무·준비DB) 반영한 4섹션: 모임 진행 순서(표)·모임 준비 체크리스트(공유 체크+주간 초기화)·간사 업무 매뉴얼·연락처/계좌. manual_sections(문서)+manual_checklist(체크) 테이블(0045, RLS read=is_staff/write=is_admin). MdView에 마크다운 표 지원 추가. 운영진 메뉴에 '운영매뉴얼' 추가.
- **2026-06-19** 링크 미리보기(OG) 텍스트 정리 — my-info·instructor-form의 og:title 이모지 제거, 설명을 카드 이미지와 중복되지 않게(용도 안내) 수정.
- **2026-06-19** 입회안내를 공지(NoticesBoard) '입회안내' 탭으로 이동 — 콘텐츠를 JoinContent 공용 컴포넌트로 분리(/join 단독 + 공지 탭 공유). 회원/관심 메뉴의 입회안내 항목 제거.
- **2026-06-19** 내 정보 입력(/my-info) 헤더 정비 — 로고를 CBMC 심볼로 교체, 제목 축소(25→20px), 안내 카피 변경('회원 명부에 등록되어 교제와 소식 전달에 사용'), '지회 회원에게만 공개' 잠금 배지 추가.
- **2026-06-19** 계좌관리 — 은행 입력을 드롭다운(국내 22개 은행 + 기타 직접입력)으로 변경.
- **2026-06-19** 입회 안내 페이지(/join) 추가 — 새서울지회 입회 절차(정기모임 3회 이상→등록 의사→지회장·신입분과 면담→단체방 초대) 먼저, CBMC 중앙회 등록·외부 안내 링크 연결. 로그인 화면 '처음이신가요?' 카드 + 회원/관심 메뉴에서 진입.
- **2026-06-19** 연간일정 최종본(2026) 구조 반영 — 사회자(host) 컬럼 추가, 형식(program)=예배/포럼/특강/기타로 정리, 표 N째 열 제거. 마이그레이션 0043(컬럼+2026 50건 시드, 수동 실행). 업/다운로드(엑셀·CSV) 컬럼 회차·날짜·요일·모드·형식·주제·강사·사회자·비고로 일치.
- **2026-06-07** 앱 전면 디자인 개편(클로드디자인 시안 이식) — 앱셸·대시보드·회원목록/상세·출석·식대·회계·공지·콘텐츠·체크인·랜딩. 모임온 톤 통일.
- **2026-06-07** 회원 사진(프로필) 기능, 출석 통계(프로그램별), GitHub 저장소(`newseoul_worklog-app`) 생성 및 Vercel 배포 시작.
- **(이전)** 마이그레이션 0001~0020로 핵심 도메인 구축 — profiles/역할·RLS → chapters/members/officers → meetings/attendance → events → archive → checkin → backgrounds → transactions → meal/checkout → announcements → meeting program → member photos.

> 참고: 상세 사고/작업 기록은 `docs/worklog/` 에 날짜별로 보존.
