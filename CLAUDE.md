@AGENTS.md

## 정본(定本) 운영 룰
- 이 프로젝트의 단일 진실은 `docs/report/정본/` 묶음이다(한장본·01-개요·02-변경이력·03-할일과-참고).
- 문서와 코드가 다르면 **항상 현재 코드가 진실**. 디자인 토큰은 `app/globals.css`, 스키마는 `supabase/migrations`에서 직접 읽는다.
- 작업할 때마다 `02-변경이력.md`에 한 줄 추가하고, 상태가 바뀌면 `한장본.md`의 "현재 상태"를 갱신한다.
- 불명확한 것은 지어내지 말고 `03-할일과-참고.md`에 "확인 필요"로 적는다.
- 갱신은 `/정본` 커맨드로 수행한다.
