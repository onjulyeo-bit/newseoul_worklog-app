-- ============================================================
-- 0033. 연도별 회비에 '회원구분(grade)' 추가 — 정회원/준회원/부부 직접 분류
-- 새서울 CBMC '아름다운 만남'
-- Supabase 대시보드 → SQL Editor 에 붙여넣어 실행. (여러 번 실행해도 안전)
--
-- 금액으로 정/준회원을 자동 판정하면 연도마다 기준 금액이 달라(예: 24년부터 20만=부부)
-- 오류가 남. → 엑셀의 '구분' 열을 그대로 저장해서 정확히 집계하고, 화면에서 수정 가능.
-- grade 예: '정회원' | '부부'(=부부정회원, 2명) | '준회원' (비우면 금액으로 추정)
-- ============================================================

alter table public.annual_dues add column if not exists grade text;
